import { EbxDateTimezonePipe } from './ebx-date-timezone.pipe';

describe('EbxDateTimezonePipe', () => {
  it('create an instance', () => {
    const pipe = new EbxDateTimezonePipe();
    expect(pipe).toBeTruthy();
  });
});
