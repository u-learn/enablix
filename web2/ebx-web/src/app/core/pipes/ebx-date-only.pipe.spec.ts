import { EbxDateOnlyPipe } from './ebx-date-only.pipe';

describe('EbxDateOnlyPipe', () => {
  it('create an instance', () => {
    const pipe = new EbxDateOnlyPipe();
    expect(pipe).toBeTruthy();
  });
});
